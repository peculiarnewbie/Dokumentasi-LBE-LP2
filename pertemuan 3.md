Cloning: download git, buka github, buka https://github.com/walbertus/line-bot-boilerplate , clone or download, buka cmd, "git clone *paste*"

install heroku, buka cmd, cd line-bot-boilerplate, heroku create.
git pull, npm install, git pull, git remote -v, git push heroku master.

set token di heroku, set secret di heroku. set webhook url link/webhook/line/v1

git status, git add src, git status, git commit -m "apapun", git push heroku master

https://developers.line.me/console/channel/1572844135/basic/
